<%= provide :title, t(".page_title") %>
<%= provide :button_text, t(".form_submit") %>
<h1><%= t ".page_title" %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>

    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t(".gravatar"), Settings.gravatar.email, target: "_blank" %>
    </div>
  </div>
</div>
